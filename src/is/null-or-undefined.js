/**
 * Evaluate item for null or undefined value.
 *
 * @param   {*}       item Item under evaluation.
 * @returns {boolean}      Results of evaluation.
 */
export const isNullOrUndefined = item => item === null || item === undefined
